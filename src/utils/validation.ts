// src/utils/validation.ts
// Validation helpers removed — CPF/CNPJ functions were unused after forms were simplified.
// If you need them in the future, reintroduce the implementations or restore from git history.

